"use server";
export type DeleteEventPayload = {};

/** Remove the event from the itinerary in the database */
const handleDeleteEvent = async ({ ...props }: DeleteEventPayload) => {};

export { handleDeleteEvent };
